apiVersion: apps/v1
kind: Services
metadata:
  name: vertex-svc
  labels:
     app:myvertexapp
spec: 
  type: Nodeport
  ports:
   -port: 8080
    nodePort: 30001
    protocol: TCP    
  selector:       
    app: myvertexapp